server:
  port: 8081
spring:
  cloud:
    config:
      server:
        git:
          default-label: eureka
          uri: https://github.com/GabrielGt-28/microservicios-tayt
          search-paths: config-data
  application:
    name: config-service